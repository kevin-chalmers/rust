// Created used cargo new hello_cargo
// This also created the Cargo.toml file
// Use cargo build to build
// Use cargo build --release to build for release
// Use cargo run to run
// Use cargo check to check if code will compile
fn main() {
    println!("Hello, world!");
}
